export const facultyNames = [
  "Faculty of Engineering",
  "Faculty of Programming",
  "Faculty of Business Adminstration",
];

export const facultyOptions = facultyNames.map((name) => ({
  label: name,
  value: name,
}));